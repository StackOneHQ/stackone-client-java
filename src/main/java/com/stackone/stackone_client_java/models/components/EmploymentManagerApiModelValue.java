/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */


package com.stackone.stackone_client_java.models.components;
import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;


/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
@JsonDeserialize(using = EmploymentManagerApiModelValue._Deserializer.class)
@JsonSerialize(using = EmploymentManagerApiModelValue._Serializer.class)
public class EmploymentManagerApiModelValue {

    public static final EmploymentManagerApiModelValue ADMIN = new EmploymentManagerApiModelValue("admin");
    public static final EmploymentManagerApiModelValue VIEWER = new EmploymentManagerApiModelValue("viewer");
    public static final EmploymentManagerApiModelValue EDITOR = new EmploymentManagerApiModelValue("editor");
    public static final EmploymentManagerApiModelValue BASIC = new EmploymentManagerApiModelValue("basic");
    public static final EmploymentManagerApiModelValue GUEST = new EmploymentManagerApiModelValue("guest");
    public static final EmploymentManagerApiModelValue UNASSIGNED = new EmploymentManagerApiModelValue("unassigned");
    public static final EmploymentManagerApiModelValue RESTRICTED = new EmploymentManagerApiModelValue("restricted");
    public static final EmploymentManagerApiModelValue UNMAPPED_VALUE = new EmploymentManagerApiModelValue("unmapped_value");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, EmploymentManagerApiModelValue> values = createValuesMap();
    private static final Map<String, EmploymentManagerApiModelValueEnum> enums = createEnumsMap();

    private final String value;

    private EmploymentManagerApiModelValue(String value) {
        this.value = value;
    }

    /**
     * Returns a EmploymentManagerApiModelValue with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as EmploymentManagerApiModelValue
     */ 
    public static EmploymentManagerApiModelValue of(String value) {
        synchronized (EmploymentManagerApiModelValue.class) {
            return values.computeIfAbsent(value, v -> new EmploymentManagerApiModelValue(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<EmploymentManagerApiModelValueEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        EmploymentManagerApiModelValue other = (EmploymentManagerApiModelValue) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "EmploymentManagerApiModelValue [value=" + value + "]";
    }

    // return an array just like an enum
    public static EmploymentManagerApiModelValue[] values() {
        synchronized (EmploymentManagerApiModelValue.class) {
            return values.values().toArray(new EmploymentManagerApiModelValue[] {});
        }
    }

    private static final Map<String, EmploymentManagerApiModelValue> createValuesMap() {
        Map<String, EmploymentManagerApiModelValue> map = new LinkedHashMap<>();
        map.put("admin", ADMIN);
        map.put("viewer", VIEWER);
        map.put("editor", EDITOR);
        map.put("basic", BASIC);
        map.put("guest", GUEST);
        map.put("unassigned", UNASSIGNED);
        map.put("restricted", RESTRICTED);
        map.put("unmapped_value", UNMAPPED_VALUE);
        return map;
    }

    private static final Map<String, EmploymentManagerApiModelValueEnum> createEnumsMap() {
        Map<String, EmploymentManagerApiModelValueEnum> map = new HashMap<>();
        map.put("admin", EmploymentManagerApiModelValueEnum.ADMIN);
        map.put("viewer", EmploymentManagerApiModelValueEnum.VIEWER);
        map.put("editor", EmploymentManagerApiModelValueEnum.EDITOR);
        map.put("basic", EmploymentManagerApiModelValueEnum.BASIC);
        map.put("guest", EmploymentManagerApiModelValueEnum.GUEST);
        map.put("unassigned", EmploymentManagerApiModelValueEnum.UNASSIGNED);
        map.put("restricted", EmploymentManagerApiModelValueEnum.RESTRICTED);
        map.put("unmapped_value", EmploymentManagerApiModelValueEnum.UNMAPPED_VALUE);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<EmploymentManagerApiModelValue> {

        protected _Serializer() {
            super(EmploymentManagerApiModelValue.class);
        }

        @Override
        public void serialize(EmploymentManagerApiModelValue value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<EmploymentManagerApiModelValue> {

        protected _Deserializer() {
            super(EmploymentManagerApiModelValue.class);
        }

        @Override
        public EmploymentManagerApiModelValue deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return EmploymentManagerApiModelValue.of(v);
        }
    }
    
    public enum EmploymentManagerApiModelValueEnum {

        ADMIN("admin"),
        VIEWER("viewer"),
        EDITOR("editor"),
        BASIC("basic"),
        GUEST("guest"),
        UNASSIGNED("unassigned"),
        RESTRICTED("restricted"),
        UNMAPPED_VALUE("unmapped_value"),;

        private final String value;

        private EmploymentManagerApiModelValueEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}
